# CSES Data Harmonization Agent - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# LLM Configuration
# =============================================================================

# Primary model for variable matching (Stage 2)
# Options: openai/gpt-4o, anthropic/claude-sonnet-4-20250514, etc.
LLM_MODEL_MATCH=openai/gpt-4o

# Model for document preprocessing (Stage 1)
# A faster/cheaper model for initial extraction
LLM_MODEL_PREPROCESS=openai/gpt-4o-mini

# Model for validation (Claude validation in dual-model architecture)
# Option 1: Use Claude CLI with your Max subscription (recommended if you have one)
# LLM_MODEL_VALIDATE=claude-cli
# Option 2: Use API key
LLM_MODEL_VALIDATE=anthropic/claude-sonnet-4-20250514

# Fallback model (used if LLM_MODEL_MATCH not set)
LLM_MODEL=openai/gpt-4o

# =============================================================================
# API Keys
# =============================================================================

# OpenAI API key (for GPT models)
OPENAI_API_KEY=sk-...

# Anthropic API key (for Claude models via API)
# Not needed if using claude-cli for validation with Max subscription
ANTHROPIC_API_KEY=sk-ant-...

# Claude CLI Setup (for LLM_MODEL_VALIDATE=claude-cli)
# 1. Install: npm install -g @anthropic-ai/claude-code
# 2. Login: claude login
# No API key needed - uses your Max subscription

# If using GESIS API or other providers:
# GESIS_API_KEY=...
# GESIS_API_BASE=https://api.gesis.org/...

# =============================================================================
# Output Configuration
# =============================================================================

# Directory for output files (default: outputs/)
# CSES_OUTPUT_DIR=outputs

# Enable verbose logging
# DEBUG=true
