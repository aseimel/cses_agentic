# CSES Data Harmonization Agent
# Complete dependencies for the workflow automation system
# PYTHON 3.14 COMPATIBLE - requires specific minimum versions

# =============================================================================
# Data Processing
# =============================================================================
pandas>=3.0.0          # 3.0.0+ required for Python 3.14 wheels
pyreadstat>=1.3.2      # 1.3.2+ required for Python 3.14 support
openpyxl>=3.1.0        # For Excel files (.xlsx) - pure Python
xlrd>=2.0.0            # For older Excel files (.xls)

# =============================================================================
# Document Parsing
# =============================================================================
lxml>=6.0.1            # 6.0.1+ required for Python 3.14 wheels (python-docx dependency)
pypdf>=4.0.0           # For PDF files
python-docx>=1.0.0     # For DOCX files (uses lxml)
striprtf>=0.0.26       # For RTF files
reportlab>=4.4.0       # 4.4.0+ required for Python 3.14 support (pure Python)

# =============================================================================
# LLM Integration
# =============================================================================
litellm>=1.60.0        # 1.60.0+ required for Python 3.14 (PR #17666)
anthropic>=0.39.0      # Claude API client
openai>=1.0.0          # OpenAI API client (for other models via LiteLLM)
tiktoken>=0.5.0        # Token counting

# =============================================================================
# Workflow Orchestration
# =============================================================================
prefect>=3.0.0         # Workflow management (optional, for advanced use)

# =============================================================================
# Utilities
# =============================================================================
python-dotenv>=1.0.0   # Environment variable management
pyyaml>=6.0.0          # YAML parsing
rich>=13.0.0           # Beautiful terminal output
click>=8.0.0           # CLI framework (alternative to argparse)
tqdm>=4.65.0           # Progress bars

# =============================================================================
# Development & Testing
# =============================================================================
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
